name: The showdown
on:
  push: main
jobs:
  if_condition:
    runs-on: ubuntu-latest
    steps:
       - run: echo "You are in Main Branch :)"
         name:IF
         if: ${GITHUB_REF##*/} == "main"
       - if: ${GITHUB_REF##*/} != "main"
         name: Else
         run: echo "You are not in main Branch :("
